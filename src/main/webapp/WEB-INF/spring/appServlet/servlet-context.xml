<beans:beans
  xmlns:mvc="http://www.springframework.org/schema/mvc"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:beans="http://www.springframework.org/schema/beans"
  xmlns:context="http://www.springframework.org/schema/context"
  xsi:schemaLocation="
    http://www.springframework.org/schema/mvc     http://www.springframework.org/schema/mvc/spring-mvc.xsd
    http://www.springframework.org/schema/beans   http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

  <!-- @RequestMapping/@RestController 등 사용 -->
  <mvc:annotation-driven />
  

  <!-- ✅ 멀티파트 파서 (prefix 반드시 beans:) -->
  <beans:bean id="multipartResolver"
              class="org.springframework.web.multipart.support.StandardServletMultipartResolver"/>
              
              
   <!--  프로퍼티 주입 활성화  -->
  <context:property-placeholder
      location="classpath:application.properties"
      ignore-unresolvable="true"/>
  

  <!-- CORS: 프론트(3000)에서 withCredentials:true로 호출 -->
  <mvc:cors>
    <mvc:mapping
      path="/api/**"
      allowed-origins="http://localhost:3000"
      allowed-methods="GET,POST,PUT,DELETE,PATCH,OPTIONS"
      allowed-headers="*"
      allow-credentials="true"
      max-age="3600"/>
  </mvc:cors>

  <!-- 컨트롤러만 스캔 (서비스/리포지토리는 root-context에서 처리) -->
  <context:component-scan
      base-package="com.app.controller,com.app.api"
      use-default-filters="false">
    <!-- @Controller @RestController 모두 포함(@RestController는 @Controller 메타) -->
    <context:include-filter type="annotation"
        expression="org.springframework.stereotype.Controller"/>
    <!-- 전역 예외/바인딩 처리 어드바이스 포함(있을 때만 효과) -->
    <context:include-filter type="annotation"
        expression="org.springframework.web.bind.annotation.ControllerAdvice"/>
  </context:component-scan>

  <!-- 정적 리소스 -->
  <mvc:resources mapping="/resources/**" location="/resources/"/>

  <!-- JSP 뷰리졸버 -->
  <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
    <beans:property name="prefix" value="/WEB-INF/views/"/>
    <beans:property name="suffix" value=".jsp"/>
  </beans:bean>
  
</beans:beans>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.mapper.UserMapper">

<!-- 
  ☆ alias 대신 FQCN
  <resultMap id="UserMap" type="com.app.domain.User">
    <id     column="ID"             property="id"/>
    <result column="PW"             property="pw"/>
    <result column="EMAIL"          property="email"/>
    <result column="ADDRESS"        property="address"/>
    <result column="TEL"            property="tel"/>
    <result column="USER_NAME"      property="userName"/>
    <result column="USER_TYPE"      property="userType"/>
    <result column="USER_STATUS"    property="userStatus"/>
    <result column="BUSINESS_NUMBER" property="businessNumber"/>
    <result column="CREATED_TIME"   property="createdTime" jdbcType="TIMESTAMP"/>
    <result column="PROVIDER"       property="provider"/>
    <result column="PROVIDER_ID"    property="providerId"/>
    <result column="TOTAL_BALANCE"  property="totalBalance" jdbcType="NUMERIC"/>
    <result column="BID_DEPOSIT"    property="bidDeposit"   jdbcType="NUMERIC"/>
  </resultMap>  
   -->
  
  <select id="findById" parameterType="string" resultType="com.app.domain.User">
  SELECT
    ID           AS id,
    PW           AS pw,
    EMAIL        AS email,
    ADDRESS      AS address,
    TEL          AS tel,
    USER_NAME    AS userName,
    USER_TYPE    AS userType,
    USER_STATUS  AS userStatus,
    CREATED_TIME AS createdTime,
    TOTAL_BALANCE AS totalBalance,
    BID_DEPOSIT   AS bidDeposit,
    PROVIDER     AS provider,
    PROVIDER_ID  AS providerId
  FROM USERS
  WHERE ID = #{id}
</select>

  <select id="findByEmail" parameterType="string" resultType="com.app.domain.User">
  SELECT
    ID           AS id,
    PW           AS pw,
    EMAIL        AS email,
    ADDRESS      AS address,
    TEL          AS tel,
    USER_NAME    AS userName,
    USER_TYPE    AS userType,
    USER_STATUS  AS userStatus,
    CREATED_TIME AS createdTime,
    TOTAL_BALANCE AS totalBalance,
    BID_DEPOSIT   AS bidDeposit,
    PROVIDER     AS provider,
    PROVIDER_ID  AS providerId
  FROM USERS
  WHERE EMAIL = #{email}
</select>

  
  <!-- User 도메인 객체 삽입 -->
  <insert id="insertUser" parameterType="com.app.domain.User">
    INSERT INTO USERS
      (ID, PW, EMAIL, ADDRESS, TEL, USER_NAME, USER_TYPE, USER_STATUS,
       CREATED_TIME, TOTAL_BALANCE, BID_DEPOSIT, PROVIDER, PROVIDER_ID)
    VALUES
      (#{id}, #{pw}, #{email}, #{address,jdbcType=VARCHAR}, #{tel,jdbcType=VARCHAR}, #{userName}, #{userType}, 
       COALESCE(#{userStatus}, 'ACTIVE'), SYSTIMESTAMP, 
       COALESCE(#{totalBalance}, 0), COALESCE(#{bidDeposit}, 0), 
       #{provider,jdbcType=VARCHAR}, #{providerId,jdbcType=VARCHAR})
  </insert>
  
  <!-- 이메일 존재 여부 -->
  <select id="existsByEmail" parameterType="string" resultType="int">
    SELECT COUNT(*)
    FROM USERS
    WHERE EMAIL = #{email}
  </select>

  <!-- ID 존재 여부 -->
  <select id="existsById" parameterType="string" resultType="int">
    SELECT COUNT(*)
    FROM USERS
    WHERE ID = #{id}
  </select>
  
    <!-- 판매자 가입: USERS 테이블에 바로 INSERT -->
	
  <insert id="insertSeller" parameterType="map">
    INSERT INTO USERS
     (ID, PW, EMAIL, ADDRESS, TEL, USER_NAME, USER_TYPE, USER_STATUS,
      BUSINESS_NUMBER, CREATED_TIME, TOTAL_BALANCE, BID_DEPOSIT, PROVIDER, PROVIDER_ID)
    VALUES
     (#{id}, #{pw}, #{email}, #{address,jdbcType=VARCHAR}, #{tel,jdbcType=VARCHAR}, #{userName},
      'SELLER', 'PENDING', #{brn,jdbcType=VARCHAR}, SYSTIMESTAMP, 0, 0, NULL, NULL)
  </insert>
  
  <!-- 구매자 가입 -->
<insert id="insertBuyer" parameterType="map">
  INSERT INTO USERS
    (ID, PW, EMAIL, USER_NAME, ADDRESS, TEL,
     USER_TYPE, USER_STATUS, CREATED_TIME, TOTAL_BALANCE, BID_DEPOSIT, PROVIDER, PROVIDER_ID)
  VALUES
    (#{id}, #{pw}, #{email}, #{name}, #{address,jdbcType=VARCHAR}, #{tel,jdbcType=VARCHAR},
     'BUYER', 'ACTIVE', SYSTIMESTAMP, 0, 0, NULL, NULL)
</insert>


<!-- 비밀번호 재설정 -->
<update id="updatePasswordByEmail" parameterType="map">
  UPDATE USERS
     SET PW = #{pw}    	 
   WHERE EMAIL = #{email}
</update>

<!-- SNS -->
 <select id="findByProvider" parameterType="map" resultType="com.app.domain.User">
  SELECT ID AS id, PW AS pw, EMAIL AS email, ADDRESS AS address, TEL AS tel,
         USER_NAME AS userName, USER_TYPE AS userType, USER_STATUS AS userStatus,
         BUSINESS_NUMBER AS businessNumber, CREATED_TIME AS createdTime,
         TOTAL_BALANCE AS totalBalance, BID_DEPOSIT AS bidDeposit,
         PROVIDER AS provider, PROVIDER_ID AS providerId
    FROM USERS
   WHERE PROVIDER = #{provider}
     AND PROVIDER_ID = #{providerId}
</select>

  <update id="linkProviderByEmail">
    UPDATE USERS
       SET PROVIDER = #{provider},
           PROVIDER_ID = #{providerId}
     WHERE EMAIL = #{email}
  </update>

  <!-- SNS 자동가입: USER_TYPE은 NULL로 저장하여 역할 선택 유도 -->
<insert id="insertSnsUser" parameterType="map">
  INSERT INTO USERS
    (ID, EMAIL, USER_NAME, ADDRESS, TEL,
     USER_TYPE, USER_STATUS, CREATED_TIME,
     TOTAL_BALANCE, BID_DEPOSIT, PROVIDER, PROVIDER_ID, PW)
  VALUES
    (#{id},
     #{email,jdbcType=VARCHAR},
     #{userName,jdbcType=VARCHAR},
     #{address,jdbcType=VARCHAR},
     #{tel,jdbcType=VARCHAR},
     NULL,                                  <!-- ★ 여기! 바인딩 안하고 상수 NULL -->
     'ACTIVE',
     SYSTIMESTAMP,
     0, 0,
     #{provider,jdbcType=VARCHAR},
     #{providerId,jdbcType=VARCHAR},
     COALESCE(#{pw,jdbcType=VARCHAR}, '')
    )
</insert>

 <!-- provider 존재 여부 -->
	<select id="existsByProvider" resultType="int">
	  SELECT COUNT(*)
	  FROM USERS
	  WHERE PROVIDER = #{provider}
	    AND PROVIDER_ID = #{providerId}
	</select>

  <!-- SNS 유저 사용자, 판매자 선택 -->
  <update id="updateRole">
  UPDATE USERS
     SET USER_TYPE = #{role},
         USER_STATUS = CASE WHEN #{role} = 'SELLER' THEN 'PENDING' ELSE 'ACTIVE' END
   WHERE ID = #{id}
</update>
  
  

</mapper>

<?xml version="1.0" encoding="UTF-8"?>						
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"						
					"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
					
<mapper namespace="com.app.dao.auction.BidDAO">

	
	<insert id="saveBid" parameterType="Bid">
		INSERT INTO bids VALUES (
			seq_bid_Id.nextval,
            #{auctionId},
            #{userId},
            #{bidPrice},
            'bid',
            SYSDATE,
            SYSDATE
        )
	</insert>
	
	<update id="updatePreviousBidsToOutbid">
        UPDATE BIDS
        SET BID_STATUS = 'outbid'
        WHERE AUCTION_ID = #{auctionId}
        AND BID_STATUS = 'bid'
    </update>
    
	<select id="getLatestBid">
		SELECT 
			B.BID_PRICE
			B.BID_TIME
			U.user_name
		FROM bids B
		INNER JOIN USERS U
		ON B.USER_ID = U.ID
		ORDER BY B.BID_TIME DESC
		FETCH FIRST 1 ROWS ONLY;
	</select>
	
</mapper>